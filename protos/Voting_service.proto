syntax = "proto3";
package protos;

message VoteRequest {
    int32 voter_id = 1;
    int32 voter_candidate = 2;
    string time = 3;
}

message VoteStatus {
    bool status = 1;
}

service VoteService {
    rpc Vote (VoteRequest) returns (VoteStatus) {}
}
